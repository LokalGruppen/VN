<?xml version="1.0" encoding="utf-8"?>
<metadata>
    <layout title="COM_VISITNORDIC_ITEM_ARTICLE" option="COM_VISITNORDIC_ITEM_ARTICLE_OPTION">
        <message><![CDATA[COM_VISITNORDIC_ITEM_ARTICLE_DESC]]></message>
    </layout>

    <!-- Add fields to the request variables for the layout. -->
    <fields name="request">
        <fieldset name="request" addfieldpath="/administrator/components/com_visitnordic/models/fields">
            <field
                    name="id"
                    type="modal_vnlist"
                    data="article"
                    required="true"
                    label="COM_VISITNORDIC_ITEM_ARTICLE_SELECT"/>
        </fieldset>
    </fields>

    <!-- Add fields to the parameters object for the layout. -->
    <fields name="params">

        <!-- Basic options. -->
        <fieldset name="basic">

        </fieldset>
    </fields>
</metadata>
